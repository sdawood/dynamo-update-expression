const {
    patches,
    diff,
    getUpdateExpression,
    getVersionedUpdateExpression,
    getVersionLockExpression
} = require('./dynamo-update-expression');

module.exports = {
    patches,
    diff,
    getUpdateExpression,
    getVersionedUpdateExpression,
    getVersionLockExpression
};
